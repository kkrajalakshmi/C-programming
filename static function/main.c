#include <stdio.h>
#include <stdlib.h>
int sum(int,int);
int main()
{
    int a=2,b=4,result;
    result=sum(a,b);
    printf("The addition of two integers is : %d\n",result);
    return 0;
}
